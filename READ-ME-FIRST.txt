UPLOAD THIS ONE — IT REPLACES EVERY EARLIER ZIP
===============================================

Everything outstanding, merged into one consistent set. It supersedes every zip
I have sent before. Upload this and ignore those — mixing them would undo work,
because they contain different versions of the same files.

HOW TO UPLOAD

Unzip it. Open the folder you get. Drag the src, supabase and tools folders
THAT ARE INSIDE IT into the GitHub uploader at the ROOT of the Fellowship repo
(Add file -> Upload files).

  DO drag:   src   supabase   tools
  DO NOT drag the folder that contains them.

.gitignore sits at the top level too. Optional — it stops node_modules and
build output showing as untracked.

WHAT IS IN IT

1. Renaming an approach (new)
   - "Rename" beside "New approach" in the authoring panel. Type anything;
     every marker in that approach moves to the new name.
   - So the triceps chips can read Lateral Head / Long Head / Medial Head
     instead of leaving the lateral head under "Standard".
   - Refused if the name is already used for that muscle or study, and only
     offered on approaches whose markers you are allowed to edit.

2. Switching between approaches (new)
   - Approach chips under the summary bar, visible to everyone, not just
     faculty. Choosing one swaps the marker on the model and the note beside
     it. EMG now shows each approach's authored label and note, as nerve
     conduction already did.

3. "Centre" button (new)
   - Slides the view so the highlighted muscle sits in the middle, keeping
     your angle and zoom. Next to "Reset view".

4. Library
   - Read / Download / Remove keep their own right-hand column and never wrap
     under the title. File name out of the subtitle; size moved beside the
     buttons.

5. Supervisors can publish their own atlas markers
   - Approve, edit and remove markers they placed — not anyone else's.
   - Fixes a Delete button that looked like it worked but did nothing.

6. Library and Feedback review restored to the nav

7. The 3D atlas work from the last few days
   - NCS highlights the study's recording muscle instead of click-to-select;
     study search and grouping by nerve; panning; cathode degree readout and
     the cathode-to-G1 distance feature both removed.

DATABASE

Already applied. Nothing for you to run. The migration file is here so the
repo's history matches the database.

AFTERWARDS

Open the sidebar and check Library and Feedback review are both there.
