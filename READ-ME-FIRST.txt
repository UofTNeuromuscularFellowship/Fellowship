FIXES + ULTRASOUND ARTEFACTS SECTION
====================================

This replaces the "ultrasound-artefacts" zip — it contains those two files AND
the three fixes. Upload this one; you do not need the other.

No database change.

HOW TO UPLOAD (GitHub web)
--------------------------
1. Open https://github.com/UofTNeuromuscularFellowship/Fellowship
2. Add file -> Upload files
3. Extract this zip and DRAG THE "src" FOLDER onto the page.
   Do not drag the folder that contains it, and do not drag this readme.
4. Commit. Vercel redeploys on its own.

WHAT IS IN IT
-------------
src/lib/drawGesture.ts             NEW - shared touch/pen/mouse drawing input
src/components/caseMedia/
      SignaturePad.tsx             signing with a finger on iOS
      Annotator.tsx                same fix for the annotation canvas
src/components/ProtectedRoute.tsx  remembers where you were going
src/pages/Login.tsx                returns you there after signing in
src/lib/caseMedia.ts               "pending review" note removed from consent
src/pages/UltrasoundPrimer.tsx     artefacts section; anisotropy note removed
src/data/ultrasoundPrimer.ts       the four Radiopaedia citations

AFTER IT DEPLOYS
----------------
If the home-screen icon still opens the dashboard, delete the icon and add it
again FROM the Waveforms & images page — an icon added earlier is still
pointing at whatever page it was created from.

public/ultrasound is untouched and is not in this zip.
