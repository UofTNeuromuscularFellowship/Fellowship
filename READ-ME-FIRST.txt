RESTORES THE LIBRARY (AND FEEDBACK REVIEW) NAV
==============================================

Two files. Unzip, open the folder, and drag the src folder THAT IS INSIDE IT
into the GitHub uploader at the ROOT of the Fellowship repo
(Add file -> Upload files).

  DO drag:   src
  DO NOT drag the folder that contains it.

  src/App.tsx                    (replaces existing)
  src/components/AppShell.tsx    (replaces existing)

WHAT HAPPENED

The Library page, its database table, the private file bucket and the seven
documents on the shelf were all still there. What went missing was the way in:
the /library route and its nav item. The 3D atlas files I sent on 21 Aug
included App.tsx and AppShell.tsx copied from a checkout made BEFORE the
library was built, so uploading them removed the route and the nav entry.
Same for the director-only Feedback review page.

These two files put both back, exactly as they were, and keep the atlas
changes.

Nothing to change in the database.
